version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - ./logs:/app/logs
      - ./.env:/app/.env
    environment:
      - NODE_ENV=development
